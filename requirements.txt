# Unified Control System Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
websockets>=11.0.0
aiohttp>=3.8.0
aiofiles>=23.0.0

# System monitoring and process management
psutil>=5.9.0

# HTTP client for device file downloads
requests>=2.28.0

# Optional but recommended for production
cryptography>=41.0.0  # For enhanced security features
uvloop>=0.17.0; sys_platform != "win32"  # Better asyncio performance on Unix